import { ActionState } from '@/features/ActionBar';
import { ApplicationDetails } from '@/types';

type ListLayoutProps = {
  data: ApplicationDetails[];
  action: ActionState;
};

export function ListLayout({ data, action }: ListLayoutProps) {
  return <></>;
}
