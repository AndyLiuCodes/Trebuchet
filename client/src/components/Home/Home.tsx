import { Box } from '@mui/material';
import { ContentSquare } from '@/features/ContentContainer/';

export function Home() {
  return (
    <Box
      sx={{
        paddingX: 5,
      }}
    >
      <ContentSquare />
    </Box>
  );
}
